//Interface that I made for targets, but obviously could be used for something else later down the line.
//This is the first interface I have ever written and I am pretty sure that it is just saying
//that anything that is "IDamageable" will work if whatever method down below is called.
public interface IDamageable
{
    void takeDamage(float damageAmount);
}